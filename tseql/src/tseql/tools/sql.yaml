description:
  "Generate a valid SQLite query for the table.
  Rules:
  1. Always write valid operation on the table.
  2. Rows are samples, columns are features.
  Schema:
  Table: soybean
  Columns:
  date,plant-stand,precip,temp,hail,crop-hist,area-damaged,severity,seed-tmt,germination,plant-growth,leaves,
  leafspots-halo,leafspots-marg,leafspot-size,leaf-shread,leaf-malf,leaf-mild,stem,lodging,stem-cankers,canker-lesion,
  fruiting-bodies,external-decay,mycelium,int-discolor,sclerotia,fruit-pods,fruit-spots,seed,mold-growth,seed-discolor,
  seed-size,shriveling,roots,class"

parameters:
  type: object
  properties:
    sql_query:
      type: string
  required:
    - sql_query

artifact_name: sql_result
